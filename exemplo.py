from InstagramPy import *

client = InstagramPy("user")

profile = client.profile()

#client.follower(profile.id)
#client.unfollower(profile.id)
#client.get_followers(profile.id)
#client.get_followersV2(profile.id)
